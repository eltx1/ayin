# Copy to /home/ayin/env/api.env and chmod 600. Never commit real values.
NODE_ENV=production
API_HOST=127.0.0.1
PORT=4000
CORS_ORIGIN=https://ayin.stream
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/ayin?schema=public

# Add the production auth/session, R2/media, analytics, mail and advertising
# variables required by the enabled AYIN modules. Keep every secret outside git.
