# Copy to /home/ayin/env/web.env and chmod 600. Never commit real values.
NODE_ENV=production

# Add only the public/runtime values actually consumed by the web application.
# Secrets belong in the API/server environment, never NEXT_PUBLIC_* variables.
