# Copy to /home/ayin/env/web.env and chmod 600. Never put secrets in this file.
NODE_ENV=production
NEXT_PUBLIC_API_BASE_URL=https://api.ayin.stream
NEXT_PUBLIC_MEDIA_BASE_URL=https://media.ayin.stream

# NEXT_PUBLIC_* values are embedded into browser-accessible application code at build time.
# Never add passwords, private keys, access keys, bearer tokens, or other secrets here.
